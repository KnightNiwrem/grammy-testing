description: A Bot API `MenuButton`.
oneOf:
  - type: object
    additionalProperties: false
    required:
      - type
    properties:
      type:
        type: string
        enum:
          - commands
          - default
  - type: object
    additionalProperties: false
    required:
      - type
      - text
      - web_app
    properties:
      type:
        type: string
        const: web_app
      text:
        type: string
      web_app:
        type: object
        additionalProperties: false
        required:
          - url
        properties:
          url:
            type: string
