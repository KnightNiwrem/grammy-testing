type: object
additionalProperties: false
required:
  - ok
  - result
  - description
properties:
  ok:
    type: boolean
    const: true
  result:
    type: boolean
    const: true
  description:
    type: string
    enum:
      - Webhook was set
      - Webhook is already set
      - Webhook was deleted
      - Webhook is already deleted
