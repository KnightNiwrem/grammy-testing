type: object
additionalProperties: false
required:
  - ok
  - result
properties:
  ok:
    type: boolean
    const: true
  result:
    type: object
    additionalProperties: false
    required:
      - message_id
    properties:
      message_id:
        type: integer
        minimum: 1
        description: The new message's ID in the bot's chat.
